# Web App
Minimal Vite + React War Room dashboard for live negotiation events.

## Run
```powershell
cd apps/web
npm install
npm run dev
```

Open:
`http://localhost:5173/?sessionId=<negotiation_session_id>`
