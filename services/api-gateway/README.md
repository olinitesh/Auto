# API Gateway
FastAPI service exposing negotiation and War Room session endpoints.

## Routes
- `GET /health`
- `POST /negotiations/start`
- `POST /negotiations/{session_id}/autonomous-round`
- `GET /negotiations`
- `GET /negotiations/{session_id}`
